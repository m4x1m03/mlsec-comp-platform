# PostgreSQL
# (Currently hardcoded in docker-compose.yaml)

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Docker Configuration
DOCKER_GID=999

# Gateway Secret
# (Currently hardcoded in docker-compose.yaml, could be moved here if needed)
GATEWAY_SECRET=welovemarcus

# VirusTotal API key (for attack similarity evaluation)
VIRUSTOTAL_API_KEY=

# Email MFA (SMTP) configuration
AUTH_EMAIL_MFA_ENABLED=true
# AUTH_EMAIL_MFA_DELIVERY: "log" for dev, "smtp" for production
AUTH_EMAIL_MFA_DELIVERY=log
AUTH_EMAIL_MFA_CODE_TTL_MINUTES=10
AUTH_EMAIL_MFA_MAX_ATTEMPTS=5
AUTH_EMAIL_MFA_FROM=no-reply@mlsec.local
AUTH_EMAIL_MFA_SUBJECT=Your MLSEC login code
AUTH_EMAIL_MFA_BASE_URL=http://localhost:4321
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=karl.j.farrar@tamu.edu
SMTP_PASSWORD=dhtg pklx qmeu jsnl
SMTP_USE_TLS=true
SMTP_USE_SSL=false
